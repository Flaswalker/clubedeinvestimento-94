[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  SECRETS_SCAN_OMIT_PATHS = "src/integrations/supabase/client.ts,src/lib/supabase.ts"

# Adicione apenas esta seção nova:
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"  # ou "zisi" (padrão)
